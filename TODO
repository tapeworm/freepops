For future versions (wishes)
============================

 * regression tests maybe with netcat or other to make it possible to run 
   some tests at least over some plugin
 * porting pocketpc. lack of a free cross compiler and emulator
 * ipv6, maybe when the switch to APR/glib will be considered this will be 
   automatically gained
 * utf8 - mimer - lstring lua utf8 support
 * add proxy section in config.lua e integrate win-IE settings discovery 
   code from LiberoPOPs
 * customize error messages
